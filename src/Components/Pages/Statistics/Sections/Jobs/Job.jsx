

const Job = ({job }) => {
      const {job_title, job_type, location, logo, remote_or_onsite, salary, job_description, company_name} = job;



      console.log(location)
      
      return (
            <div>
                  bismillah
            </div>
      );
};

export default Job;